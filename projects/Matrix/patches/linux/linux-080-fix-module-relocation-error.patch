diff -Naur linux-ubuntu-omap4-2.6.35-903.19.orig//arch/powerpc/Makefile linux-ubuntu-omap4-2.6.35-903.19/arch/powerpc/Makefile
--- linux-ubuntu-omap4-2.6.35-903.19.orig//arch/powerpc/Makefile	2010-11-21 16:24:21.275402003 +0100
+++ linux-ubuntu-omap4-2.6.35-903.19/arch/powerpc/Makefile	2010-11-21 16:24:35.985401998 +0100
@@ -112,11 +112,6 @@
 # kernel considerably.
 KBUILD_CFLAGS += $(call cc-option,-funit-at-a-time)
 
-# FIXME: the module load should be taught about the additional relocs
-# generated by this.
-# revert to pre-gcc-4.4 behaviour of .eh_frame
-KBUILD_CFLAGS	+= $(call cc-option,-fno-dwarf2-cfi-asm)
-
 # Never use string load/store instructions as they are
 # often slow when they are implemented at all
 KBUILD_CFLAGS		+= -mno-string
diff -Naur linux-ubuntu-omap4-2.6.35-903.19.orig//Makefile linux-ubuntu-omap4-2.6.35-903.19/Makefile
--- linux-ubuntu-omap4-2.6.35-903.19.orig//Makefile	2010-11-21 16:24:16.855402007 +0100
+++ linux-ubuntu-omap4-2.6.35-903.19/Makefile	2010-11-21 16:24:35.985401998 +0100
@@ -588,6 +588,9 @@
 # disable invalid "can't wrap" optimizations for signed / pointers
 KBUILD_CFLAGS	+= $(call cc-option,-fno-strict-overflow)
 
+# revert to pre-gcc-4.4 behaviour of .eh_frame
+KBUILD_CFLAGS	+= $(call cc-option,-fno-dwarf2-cfi-asm)
+
 # conserve stack if available
 KBUILD_CFLAGS   += $(call cc-option,-fconserve-stack)
 
