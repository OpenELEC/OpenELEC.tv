[Unit]
Description=Amlogic IR remote support

[Service]
Type=oneshot
ExecStart=/bin/sh -c '[ -f /storage/.config/remote.conf ] && /usr/bin/amremote_config /storage/.config/remote.conf || /usr/bin/amremote_config /etc/remote.conf'
RemainAfterExit=yes

[Install]
WantedBy=basic.target
